/**
 * @rick/application
 *
 * Application layer: use cases, commands, queries, and orchestration.
 * Depends on domain; infrastructure-free.
 *
 * Sprint 0 stub — use cases will be introduced in Sprint 1+.
 */

export type { ProjectId, ContractId, RunId, Result } from '@rick/domain'
